import {{properCase name}} from './main/index.js'

{{!-- {{properCase name}}.install = (Vue ) => {
Vue.component({{properCase name}}.name, {{properCase name}})
} --}}

const install = function (Vue) {
  Vue.directive('{{kebabCase name}}', {{properCase name}})
}

export default install
